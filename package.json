{
  "name": "nano-cortex-gig-view",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Stage-friendly live gig view for the Neural DSP Nano Cortex over Web Bluetooth",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/web-bluetooth": "^0.0.21",
    "jsdom": "^25.0.1",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
